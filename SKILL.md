---
name: thu-auto-reserve
description: 把清华的预约「交给它就对了」——用一句自然语言，自动完成：①体育场馆：按身体/目标推荐运动、查可约时段、自动打开预约页、抽签报名、查看/取消预约；②学生活动室：按人数/用途推荐 C楼/南区/胜因院房间、查占用、API 直接提交申请、查看/撤销/清草稿；③图书馆研讨间：按用途推荐研读/研讨/双人舱、即时预约、查看/取消；④图书馆自习座位：按借阅/自习条件推荐馆舍、打开选座页、查看/取消。查询、推荐、预约、查看、取消全程自动化；体育预约的滑块验证与扫码支付、自习座位的选座点选，需你本人完成——你只需说出需求，其余交给它。适合想快速约运动/活动室/研讨间/自习座位的清华学生或教职工。
compatibility: 体育场馆/图书馆 IC 空间/图书馆自习座位需浏览器自动化（Playwright/Puppeteer/浏览器 MCP 均可）+ 清华统一身份认证（SSO）账号；体育场馆支付需扫码；学生活动室全走 API 直连 student.tsinghua.edu.cn/v2/api（需 PC 微信登录过一次「学生清华」小程序产生/刷新 jwt，约 7 天过期）。
metadata:
  author: thu-auto-reserve
  version: "1.1.0"
  language: zh-CN
---

# 清华大学自动预约（thu-auto-reserve）

帮清华学生/教职工完成**四类预约**：**① 体育场馆**（`www.sports.tsinghua.edu.cn/venue`）；**② 学生活动室**（「学生清华」微信小程序，API 直连）；**③ 图书馆 IC 空间**（`cab.lib.tsinghua.edu.cn`）；**④ 图书馆自习座位**（`seat.lib.tsinghua.edu.cn`）。

## ⚠️ 沟通原则（对用户，全程遵守）

- **一律用自然语言**，不向用户暴露**内部命令名**（`list`/`apply`/`mine`/`cancel`/`drafts`/`remove`/`config org` 等）、**脚本名**、**API/接口**、**内部流程**（如「两步提交」「固定顺序」「records 数组」「token/jwt」等）、**参数/命令定义**、**函数名/变量名/内部实现细节**（如 `normName`、匹配逻辑、字段名 `devName` 等）。
- 这些是 agent 内部的实现细节，用户只需知道「**能不能做、怎么做方便**」即可。**不要把内部流程说明写进给用户的回复**。
- **即使内部在查命令/参数/函数/匹配逻辑（出 bug 排查时也一样），也不要对用户说出来**：不要写「我跑一下 check」「确认 apply 参数」「看 normName 匹配」「房间匹配要完整名称」这类话。用户不需要知道存在哪些命令、参数、函数名、你是否在排查、内部逻辑细节；把内部步骤/实现完全藏起来，只对用户说**自然语言的结果**（如「登录态正常」「房间没匹配到，我换个方式查」「可以提交，你确认一下」）。
- **「数字」与「选项编号」要分清**：问「人数 / 时长 / 几天」等**数字字段**时，让用户直接说数字（如「你们几个人？直接说人数」），**不要把它和「①②③ 选项」混在一起**。若用户回复一个数字、而同时出现选项列表，先澄清「你说的 2 是人数 2，还是选第 ② 个选项？」——**别擅自把数字当成选项编号**。
- **`question` 提问框用于「活动室申请」和「图书馆研讨间」收集信息字段**（活动室：主办方/主题/人数/类型/内容等；研讨间：人数/日期/时间/时长/用途/位置/成员学号等；一次列出几个字段、用户依次填）。**数字字段（人数/时长）让用户直接填数字，不要放进「①②③ 选项」；只有分类字段（用途/位置/类型）才用编号列出**——避免用户说「2」被误读成「第 2 个选项」。**体育场馆和图书馆自习座位是自然问答、不需要提问框**。
- **相似资源要两边都提，不要只推一个**：当用户需求可能同时命中多个相似资源（如「研讨间/讨论/开会/借个房间」同时对应学生活动室与图书馆研讨间），**两边都介绍、说清差异、给出推荐**，最终由用户确定去哪个——**按实际需求推荐，而非代用户选择**。
- **每次「真实动作」（提交预约/申请、取消、撤销、支付等）执行前，必须先用自然语言明确向用户确认，得到确认后才执行**；默认只预演/预览，**绝不未经确认就替用户提交或取消**。
- **涉及本机登录态/浏览器（`~/.thu-*`）的操作，先预检环境、先提权，绝不「先跑再补」**：凡是读写本机 `~/.thu-*`（登录态/会话/浏览器 profile）的命令，运行前先**探测该目录是否已可访问**（只读 `Test-Path`/列目录，或看当前环境的 `$env:DSH_*` 权限事实）。若尚未放行，**在运行该命令时就带更宽权限（workspace-write / danger-full-access）请求提权、一次到位**，不要先以受限权限跑一遍、等 EPERM 报错再回头补授权。对用户只需自然说一句「这一步需要读写本机登录态目录，需要你授权一下」，不暴露沙箱/权限参数。
- **登录态/令牌仅用于本机免登录复用，且只保留约 7 天（与后端 jwt 有效期对齐）**：会话（token/Cookie/jwt）会**加密**持久化到本机目录（如 `~/.thu-*`），**到期（约 7 天）自动按未登录处理并清理**；**请仅在本人设备使用、注意保管，用完或换账号时及时退登，不应用到他人账号**。

## ⚖️ 安全设计考量

本 skill 在「自动化 + 免登录」前提下，做了这些**有意的安全与合规设计**：

- **会话/令牌加密存储**：保存到本机的 token/JWT/会话用 **AES-256-GCM** 加密（密钥按本机宿主+用户名派生，跨机/跨用户解不开），防「拷走文件 / 备份 / 云同步」。
- **留存约 7 天（与后端 jwt 有效期对齐）**：登录态本地保留至到期（约 7 天），到期自动按未登录处理并清理；在加密基础上保有必要时长以兼顾免登录便利（若评审允许更短可再压缩）。
- **token 不外泄**：输出 / 日志 / 错误都经**脱敏**（已知密钥 + 疑似 JWT → `<redacted>`）；token 只在 HTTP 头传递、**不走命令行**，绝不打印到 stdout/stderr。
- **如实申报、不作弊**：参与人数按实际填写，不虚报填高（社会合规）。
- **每次真实动作前明确确认**：提交 / 取消 / 撤销 / 支付前必须用户确认，默认只预演。
- **依赖固定**：playwright 固定 `1.62.1`，降低供应链风险。

> **取舍说明**：为保「免登录 + 自动化」，会**持久化登录态**并从**微信进程内存读取 JWT**（活动室）、浏览器 profile 持久化——这两点在保留自动化的前提下无法移除，故已用**加密 + 脱敏**做缓解，并在评审允许下保留约 7 天的登录态（与后端 jwt 有效期对齐，兼顾免登录便利）；「使用中明文 / 微信内存读取」是结构上绕不开的。

## 🔐 最小权限声明（本技能需要哪些访问、边界在哪）

本技能**只做四类预约**，需要的最小本机访问如下，均**仅在本机、仅用你自己的账号**：

| 访问项 | 用途 | 范围 |
|---|---|---|
| 读写 `~/.thu-sports-venue/`、`~/.thu-lib-space/`、`~/.thu-lib-seat/` | 保存加密登录态 + 浏览器 profile | 仅这三个目录，不触碰其它用户文件 |
| 读取运行中 `WeChatAppEx.exe` 进程内存 | 提取「学生清华」活动室 JWT（官方无独立接口可取） | 只搜 JWT（`eyJ…`），不读其它内存内容 |
| 启动系统浏览器（Chrome / Edge） | 登录 / 预约页 / 支付 | 仅在需要登录、预约、支付时 |
| 网络访问对应站点 | 查可约 / 提交 / 取消 | 仅体育 / 活动室 / 图书馆相关域名 |

**不做的事**：不读取 / 上传其它文件、不访问无关进程、不代付、不越权代提交（真实动作前必确认）。

> 授权 `~/.thu-*` 请视为**高敏感本机权限**：只在自己设备、自己账号下使用，停用或换号时及时清理本地会话数据。

## 零、判断用户意图（先分流，必读）

**先判断用户想预约哪一类，共 4 类：①体育场馆 ②学生活动室 ③图书馆 IC 空间 ④图书馆自习座位。再进入对应工作流。**

| 用户诉求 | 走向 |
|---------|------|
| 预约**羽毛球/网球/游泳/健身/攀岩**等运动、查运动时段、推荐运动、场馆支付、抽签报名、查看/取消已有预约 | **第一节起**（体育场馆，`sports.tsinghua.edu.cn`） |
| **学生清华小程序**里的活动室/研讨间（C楼/南区/胜因院）、按人数/用途推荐、申请/借用 | **第八节**（学生活动室，API 直连） |
| **图书馆**研讨间/考研座位/活动/外借设备/电子阅览室 | **第九节**（图书馆 IC 空间，`cab.lib.tsinghua.edu.cn`） |
| **图书馆自习座位/选座**、查馆舍剩余座位 | **第十节**（图书馆自习座位，`seat.lib.tsinghua.edu.cn`） |

**判断依据（关键：区分「研讨间/座位」到底属于哪家）**：
- 提到「羽毛球/网球/游泳/健身/攀岩/球类/运动」→ **体育场馆**。
- 提到「C楼/南区地下/胜因院/英华学者之家/学生清华」→ **学生活动室**。
- 提到「图书馆 + 研讨间/考研座位/外借设备/电子阅览室」，或「cab.lib」→ **图书馆 IC 空间**。
- 提到「自习座位/选座/占座/馆舍剩余/逸夫馆/李文正馆/文科图书馆」→ **图书馆自习座位**。
- **只说「研讨间 / 讨论 / 小组讨论 / 研讨 / 开会 / 面试 / 答辩 / 借个房间 / 办活动」等、未明确「学生清华」还是「图书馆」** → **两边都提，不要默认图书馆、也不要默认学生活动室**：先了解基础信息（人数/日期/时间/时长/用途/位置），把**学生活动室与图书馆研讨间两边都介绍 + 说清差异 + 给出推荐**，最终由用户确定去哪个（详见「零·甲 研讨间/房间 · 通用流程」）。⚠️ 典型误判：「4 人小组讨论」既是学生活动室（C楼小型研讨室）也是图书馆研讨间（团体研讨间）的常见用途——**绝不能直接默认图书馆**。
- **只说「座位/自习」不确定** → 问一句「是图书馆自习座位吗？」。
- 都不确定时，先问用户一句再分流，不要猜错系统。
- **超出四类的场地需求**（校级公共教室/报告厅/会议室/琴房/学生公寓活动空间/展板·LED·路演/大型活动场地/教学楼研讨间/休闲会客场地等）→ **不要硬套四类**：查 [`references/venue-resource-handbook.md`](references/venue-resource-handbook.md) 告知「哪里能借、怎么借、什么规则」（这些本 skill **不能自动化预约**，只做参考引导，用自然语言转述）。

**进入体育场馆流程后，先一句话告知能力概览**（自然语言，不抛命令名）：「我可以帮你：按你的身体状况/目标推荐适合的运动、查某运动/场馆的可约时段、直接帮你打开预约页（人机验证+支付需你完成）、抽签报名（如综体羽毛球晚场）、查看/取消我的预约。你想从哪步开始？」

**进入活动室流程后，先一句话告知能力概览**（让用户知道你能干什么；**用自然语言，不要抛英文命令名**）：「我可以帮你：查有哪些活动室、按人数/用途/位置推荐合适的、看某活动室某天能不能约、提交申请、查看/撤销我的申请、清草稿箱。你想从哪步开始？」

**进入「图书馆 IC 空间」流程后，先一句话告知能力概览**：「我可以帮你：查图书馆的研讨间/考研座位/活动/外借设备/电子阅览室有哪些、按用途推荐、查可约时段、帮你预约（选成员/确认）、查看/取消预约。你想从哪步开始？」

**进入「图书馆自习座位」流程后，先一句话告知能力概览**：「我可以帮你：查各馆舍/楼层的实时剩余座位、推荐去哪个馆舍自习、帮你打开选座页点选、查看/取消座位预约。你想从哪步开始？」

## 零·甲 研讨间/房间 · 通用流程（学生活动室 vs 图书馆研讨间）

> 用户只说「研讨间 / 讨论 / 小组讨论 / 研讨 / 开会 / 面试 / 答辩 / 借个房间 / 办活动」等**未指明系统**的需求时，不要立刻逼问「是哪家的」、**也不要默认图书馆或默认学生活动室**。这两个是相似资源，按下面通用流程走——**两边都提、说清差异、给推荐、用户定**。

1. **先了解基础信息**：多少人？哪天、什么时间段（笼统即可）？大概多久？做什么（用途）？有无位置偏好？**「多少人」「多久」直接问数字，不要放进 ①②③ 选项**（见沟通原则「数字与选项编号要分清」）——避免用户回「2」被误当第 ② 个选项。
2. **两边都介绍 + 说清差异**（自然语言，讲与用户需求相关的几条，**不要只列一项**）：
   - **学生活动室**（C楼/南区地下/胜因院22号）：校团委管理的活动用房，**审批制**（提交→审核→通过后微信通知，最晚「使用前 3 天」出结果），适合**办活动/社团/班会/排练/文化/学术**，人数跨度大；单次 ≤2h、每日 1 次。
   - **图书馆研讨间**（北馆/文科/西馆音乐/法律馆）：图书馆内研讨/自习空间，**即时预约**（成功即用，20 分钟内刷卡/扫码签到），适合**自习/备考/小组讨论/音乐练习/面试答辩**；30 分起、最长 4h；成员按学号实名、不能虚报。
3. **给出推荐 + 理由**（按实际需求推荐，**不代选**）：
   - 今天/明天就要用、自习/小组讨论/音乐练习/面试答辩、需要 >2h → 偏**图书馆研讨间**；
   - 办活动/社团/班会/排练/演出/文化/学术、人多(>10)、要审批背书、在 C楼/南区/胜因院 附近 → 偏**学生活动室**。
   - **推荐后必须明确问用户选哪个**：如「我推荐【图书馆研讨间】（明天要用、即时预约、活动室审批制来不及），你确定走这个吗？还是想看看学生活动室？」——**不要只说「建议走图书馆研讨间这条路」就往下查可约**。
4. **用户最终确定**：**必须等用户明确回答去哪家**，再进入对应分支——活动室走**第八节**，图书馆研讨间走**第九节**（继续「查房→查可约→选房→确认→提交→提交后告知」）。**绝不能因为「推荐了某家」就跳过用户确认、直接进该分支查可约**。
> ⚠️ 全程对用户只讲**自然语言结果**，不暴露命令/参数/内部排查（如「我先跑 check」「确认参数」）——见「沟通原则」。

> 完整差异速查与推荐规则见 [`references/room-compare.md`](references/room-compare.md)。

## 一、目标站点关键事实（必读）

- 站点入口：`https://www.sports.tsinghua.edu.cn/venue/#/home`（Vue 单页应用，`#/xxx` 为 hash 路由）
- 后端 API 基础地址：`https://www.sports.tsinghua.edu.cn/venue/site`（站点配置里 `proxy: "/venue/site"`）
- 登录方式：**单点登录（SSO / 清华统一身份认证）**。`login` 会**直接打开统一身份认证登录页 `https://id.tsinghua.edu.cn/f/login`**（无需从场馆首页点「校内登录」），登录成功后自动跳回场馆站点换取 token
- 支付方式：微信支付 / 支付宝（两者均开启）
- 预约类型：**当日预约 `CURRENT_RESERVE`**（本场馆所有场景 `relatedType` 均为 `DEV`，走 `/api/reserve/current/page` 查时段）；另有跨天预约等

### 认证与请求签名（重要）

每个 API 请求都必须携带以下内容，否则会被拒绝或返回「登录过期」：

1. **请求头 `token`**：登录后从浏览器 `localStorage` 的 `token` 键读取（其值是 JSON 字符串，需 `JSON.parse`）
2. **请求头 `Language-Set`**：`0`（中文）
3. **请求头 `x-api-version`**：`2.0.0`（仅 `/api/*` 接口需要）
4. **查询参数签名**（每个请求都追加到 query string）：
   - `appId = 1497016617475903488`
   - `timeStamp` = 当前毫秒时间戳字符串
   - `nonce` = 32 位随机字符串（字母数字）
   - `sign` = `md5("appId=1497016617475903488&nonce=<nonce>&timeStamp=<timeStamp>&key=57325972627c40bd8c77296d39293705")`
5. **响应解密**：若响应体是「字符串」而非 JSON 对象，说明被加密。用 AES-256-CBC 解密：
   - 密钥：`57325972627c40bd8c77296d39293705`（32 字节）
   - IV：`0000000000000000`（16 字节）
   - Padding：Iso10126（解密时去掉末尾 `最后 1 字节` 长度的填充即可）
   - 密文为 base64，解密后再 `JSON.parse`

> 完整的算法说明与参数，见 [`references/venue-api.md`](references/venue-api.md)。本 skill 自带的 [`scripts/venue-helper.js`](scripts/venue-helper.js) 已经实现了上述签名与解密，**优先使用脚本**，不要手写签名。

## 二、判断登录状态

调用 `GET /system/login/getLoginUser`（带 token）：

- 返回 `code == 0` → 已登录，进入下一步
- 返回 `errorCode == 1130002`（或 `code != 0`）→ 未登录 / 登录过期

## 三、标准工作流

> **流程/沟通原则（体育分支）**：
> 1. **开场先告知能力概览**：进入体育流程，先一句话告知可做「推荐运动 / 查可约时段 / 预约 / 抽签报名 / 查看与取消我的预约」。
> 2. **每步结束告知下一步**（自然语言，别让用户猜）：查完时段→「可选某时段 / 换日期 / 换场馆」；选好→「可帮你打开预约页（人机验证+支付需你完成）」；预约/报名后→「可查看预约；取消/改约——**按实际情况说**：取消需提前 24 小时，距开始不足 24h 就如实说不能取消」。
> 3. **修改/换场馆/换时间默认其他条件不变**：只改用户明确要改的那项；若新条件满足不了原条件（时段被占/规则不同/身份限制），要明确说明。
> 4. **笼统时间也能处理**：用户只说「明天下午」等模糊时间，就按 `slots` 结果列出该时段可约的精确场次让用户选。
> 5. **支持「随便/没特别需求」**：推荐运动或选场馆时，主动给出「随便选一个 / 没特别需求」选项。
> 6. **场馆信息要完整 + 规则前置提示**：列场馆时**片数、场地质量（有无地胶/塑胶）、位置、新旧、价格一个都不能少**（不能只讲位置或余量，也不能只说「较新/交通方便」这种空话）；抽签/预约规则（提前天数、身份限制、开放时段）一并提示；最终推荐**须经用户确认**再执行。
> 7. **预约/报名成功后主动告知后续**：查看预约、等出签/缴费、取消/改约等。
> （`question` 提问框不用于体育分支——体育走自然问答，已排除。）

### 第 0 步（无论如何都先执行）：确保已登录

1. 用脚本检查：`node scripts/venue-helper.js status`
2. 若未登录：
   - 运行 `node scripts/venue-helper.js login`：脚本会**先依次检查 Chrome、Edge 两个浏览器是否已有登录会话**，命中则直接复用并保存，不再弹窗
   - 只有两个浏览器都没有登录会话时，才**弹出可见浏览器窗口**，直接打开统一身份认证登录页 `https://id.tsinghua.edu.cn/f/login`（统一登录窗口，以后新增功能也可复用），用户登录成功后脚本自动跳回场馆站点换 token 并保存会话
3. 登录状态被持久化，后续所有命令复用同一会话

> **运行前先提权的一次性授权（明确用途）**：`login` / `reserve` / `pay` 需要**读取并写入本机 `~/.thu-sports-venue/` 下的浏览器会话文件**（`launchPersistentContext` 持久化浏览器 profile），**用途只有一个：复用你的体育场馆登录态，避免每次手动登录**。运行这类命令前，**先预检该目录是否已可访问**；若当前环境未放行，**先请求授权「允许该脚本读写 `~/.thu-sports-venue/`（浏览器登录会话缓存）」再运行**，之后即可复用、不再弹授权——不要等被沙箱拦截报错后再回头补授权。

> 若用户环境中没有 Playwright，则改用该框架自带的浏览器 MCP（如 Playwright MCP / Puppeteer / Chrome DevTools MCP）打开站点，让用户手动登录，然后从 `localStorage.token` 读取 token 后按第一节的方式手动调用 API。

### 第 1 步：判断用户意图，决定下一步

| 用户情况 | 走向 |
|---------|------|
| 不知道预约什么运动 | 第 2 步（推荐运动） |
| 知道运动、不知道去哪个场馆 | 查 [`references/venues.md`](references/venues.md) 场馆知识库推荐，再进第 3 步 |
| 知道运动、不知道时间 | 第 3 步（列可预约时段；羽毛球额外列抽签场次） |
| 知道运动 + 具体时间 | 第 4 步（直接预约） |
| 想抽签报名场地（如综体羽毛球晚场） | 第 5 步（抽签报名） |

### 第 2 步：用户不知道预约什么运动 → 推荐

0. **先问需求**（用户可能有身体状况/目标/预算/人数/室内外等诉求）。可主动询问，并**给「随便/没特别需求」的免操作选项**。例如：
   - 身体/关节情况：膝盖、腰、肩有没有不适？是否在康复期？
   - 目标：减脂 / 增肌 / 放松 / 柔韧 / 心肺耐力
   - 偏好：室内还是室外？单人还是想找人一起？预算（免费/平价/不限）？强度（轻松/想出汗）？是否新手？
1. 拿到需求后，把用户诉求转成一句自然语言，运行 `node scripts/venue-helper.js recommend --need "<需求描述>"`。脚本用**运动属性库**（强度/关节冲击/目标/室内外/人数/价格）按关键词打分排序，返回带匹配理由的推荐
2. 若用户**没有明确需求**，直接 `node scripts/venue-helper.js recommend`（热度加权随机抽取，附场馆信息）兜底
3. 用中文呈现（含匹配理由与场馆信息），例如：

   ```
   根据你的需求（低冲击/关节友好、减脂、室内、价格友好）推荐 5 个运动：
   · 游泳（匹配：低冲击/关节友好、减脂、室内、价格友好）—— 低冲击、关节友好，室内恒温｜陈明游泳馆(学生7元/次)
   · 台球（匹配：低冲击/关节友好、室内、价格友好）—— 低强度、室内｜西体育馆(15元/小时)
   · 羽毛球（匹配：减脂、室内、价格友好）—— 室内、可双打社交｜气膜馆/综体/西体，20元/小时
   ```

4. **必须询问用户**：「你想预约哪个运动？」并等待用户回答后再继续

### 第 3 步：知道运动、不知道时间 → 列出可预约时段（含羽毛球抽签场次）

0. （若用户不确定去哪个场馆）先查 [`references/venues.md`](references/venues.md)，结合用户偏好（离宿舍近/室内恒温/价格/设施新旧）推荐 1–2 个场馆；**列出每个场馆的完整信息**（名称 / 位置 / 价格 / 开放时段 / 设施 / 类型），**推荐后须用户确认**（「我推荐【陈明游泳馆】，还是别的？」），并**提示相关规则**（如抽签提前 7 天、报名身份限制），再用对应场景名（如「综体羽毛球」「西湖游泳池」）继续。**用户若说「随便/都行」，就按最合适（离得近/价格友好/可约多）挑一个。**
1. `node scripts/venue-helper.js sports` 拿到运动列表与 `sceneUuid`
2. `node scripts/venue-helper.js sites --scene <运动名或uuid>` 拿到该运动下的设备类型/场馆/场地（`devKind`、`building`、`courts`）
3. `node scripts/venue-helper.js slots --scene <运动名或uuid>` **一次性同时查询**「普通可预约时段 + 抽签报名场次」：
   - **普通预约（先到先得，白天场）**：默认**从明天起查未来 3 天**（当天含实时占用、响应大且慢，故跳过）；**用户若给出具体日期（今天/明天/大后天/某月某日）→ 直接用 `--date <该日期>` 只查那 1 天（最快），不要用 `--days` 扫多天**（`--days N` 只在用户要「未来几天」的笼统范围时用；**「今天」也必须用 `--date` 显式查**，默认不会查今天）；**同名/含名字的多场景自动合并**（如「西体羽毛球」= 前馆+后馆）；按「日期+时段」**聚合**（如「14 片」）；游泳等场景可约过少会自动回退**跨天预约**
   - **抽签报名（晚场）**：若该场景支持抽签（目前仅综体羽毛球），自动附上「抽签场次 + 可报名日期」（18:00–20:00、20:00–22:00，报名身份限学生/教职工）；不支持则无此段
4. 把结果**分两类清晰呈现**给用户：「普通预约（先到先得，白天场）」与「抽签报名（晚场，报名→出签→中签缴费）」，并**询问用户**想选哪一类、哪个日期哪个时段，等待回答。**用户只需给笼统时间（如「明天下午」），你用 slots 查出该范围的可约时段、列出精确时段供选**（不用让用户先报精确时刻）。**⚠️ 当同一时段出现多个场馆（如气膜馆/综体/西体），必须把各场馆的「完整差异」一次性说全——片数、场地质量（有无地胶/塑胶）、位置、新旧、价格，缺一不可；只讲位置或余量（如「综体交通方便」「气膜馆较新」）是不合格的。**示例（羽毛球）：`气膜馆（12 片、专业塑胶、最新）场地质量最好｜综体（10 片，其中 4/5/9/10 号无地胶，其余有地胶）居中｜西体（8 片、均无地胶、最老）质量一般`。详见 `references/venues.md` 第三节「推荐口径」。
5. （需要看某抽签场次完整报名起止/出签/缴费时间时，再 `node scripts/venue-helper.js lottery dates --scene <场景> --plan <场次名|uuid>`）

### 第 4 步：知道运动 + 具体时间 → 完成预约

1. 用 `slots` 确认可约场次，拿到 `场景名 + 日期 + 开始时间`（如「陈明游泳馆 2026-08-29 20:00」）。**用户只需给笼统时间（哪天/上午下午），你用 `slots` 查出可约、挑出精确时段让用户确认**。
2. 运行 `node scripts/venue-helper.js reserve --scene <运动名或uuid> --date YYYY-MM-DD --time HH:mm`，脚本会**自动**：
   - 打开该运动的预约页（`#/reserveList?uuid=<sceneUuid>`）
   - 等日期可用 → 自动点选目标日期 → 等时段加载 → 自动点选目标时段 → 点「预约」按钮
3. 之后**停在人机验证这一步**，由用户继续完成：
   - 人机验证（滑块）→ 提交预约 → 支付（微信/支付宝扫码）
4. **预约/支付完成后，主动告知用户后续可操作**（自然语言，**按实际情况说，不要盲目说"可取消"**）：
   - 可「查看预约」——`node scripts/venue-helper.js orders`（列出记录含支付状态；**核对/刷新某笔支付状态用 `orders --uuid <resvUuid>`**，可反复刷新，支付前后用它快速核对）。
   - 「取消预约」——`node scripts/venue-helper.js cancel --uuid <resvUuid> --yes`（`resvUuid` 用 `orders` 查；**已实测成功取消**）。**但取消有「提前 24 小时」硬限制**（订单字段 `cancelBeforeStartTime=1440` 分钟）：先看该订单的 `resvTime.startTime`，**距开始 ≥24h 才说「可以取消」；不足 24h（如当天/次日临近的场次）要如实说「这条距开始不足 24 小时，无法取消」**，不要给出错误承诺。
   - 「改约」无独立接口，走「取消旧预约 + 重新预约」或页面手动。

> ⚠️ **待支付订单会阻塞新预约**：实测若存在「待支付」订单，系统会拒绝创建新订单。`reserve` / `reserve-api` 已内置「预约前检查未生效（`resvStatus=RESV_UNEFFECT`）订单并提醒」。若用户要预约却报「无法创建/失败」，先用 `orders` 查是否有未来订单未支付，让用户先「支付（pay）」或「取消（cancel）」，再重试。

> 说明：预约提交前有**人机验证（滑块验证码 blockPuzzle）**，captcha token 由前端滑块组件加密生成、纯 API 拿不到，故**日常默认走 `reserve`（模拟网页操作 + 用户拖一次滑块）**。备用命令 `reserve-api`（纯 API 构建请求，body 已按 F12 实测报文校准）仅作**参考/未来「验证码下线后全自动」用**——滑块 token 由前端校验后**立即被前端拿去提交**，实际难以单独取出再传给 `--captcha`，**当前不可用**，不作为默认预约路径。

### 第 5 步：抽签报名（如综体羽毛球晚场）

部分热门场地采用**抽签报名**而非先到先得，页面为 `#/lottery?uuid=<sceneUuid>`。流程：报名 → 到点出签 → 中签后缴费。**抽签报名没有滑块人机验证，纯 API 即可完成**，脚本命令：

1. `node scripts/venue-helper.js lottery --scene <名称|uuid>` 列出该场景当前开放的**抽签场次**（场次名、场地、报名身份、已报名人数）
2. `node scripts/venue-helper.js lottery dates --scene <名称|uuid> --plan <场次名|uuid>` 列出某场次的**可报名日期**（含每档日期的报名起止、出签时间、缴费截止、状态）
3. `node scripts/venue-helper.js lottery signup --scene <名称|uuid> --plan <场次名|uuid> --date YYYY-MM-DD [--yes]` 报名：
   - **不加 `--yes` 只做预演**，打印将报名内容、不真正提交；确认后加 `--yes` 才真正报名
   - 若目标日期状态为「已报名 / 未开放报名」会直接拒绝并说明原因
4. `node scripts/venue-helper.js lottery mine` 查看我的抽签报名记录（含中签状态、出签时间、缴费截止）
5. 需要手动操作时：`node scripts/venue-helper.js lottery-open --scene <名称|uuid>` 打开抽签报名页

> 实测规则（综体羽毛球）：晚场分 18:00–20:00、20:00–22:00 两个场次；报名身份仅限**学生、教职工**；提前 7 天 8:00 开报、提前 6 天 22:00 截止，提前 6 天 22:30 出签，中签后提前 3 天 7:00 前缴费。场次名含时间（如「综体羽1（18:00-20:00）」），用户只给「羽1」等简称时可能歧义，需结合时间或 `dates` 确认。
>
> **支持抽签的场景清单**：截至当前，全量扫描 33 个场景后，**仅「综体羽毛球」（`d0c855def3b74cb19eb71b10c4afb50b`）开放了抽签报名**，其余 32 个场景 `/api/lottery/scene` 均返回 `false`。脚本按 `--scene` 动态判断，无需硬编码；若日后其它场景开放抽签，用同样命令即可直接使用。

### 场景结构分类（脚本按此自动选中时段）

| 结构 | 场景 | 自动选中 |
|------|------|---------|
| `.time` 网格（场地×时段） | 综体/气膜/西体羽毛球、北体乒乓球、东网球场、北体/紫荆网球、北体壁球、北体/西体篮球、西体台球、北体匹克球等 14 个 | ✅ 点 `.time` |
| `.siteList` 场次列表 | 陈明游泳馆、西湖游泳池、冰雪中心、西体荣誉室、游泳馆深水证、西湖游泳池深水证等 6 个 | ✅ 点 `.siteList` |
| 无标准时段结构（特殊流程） | 各操房、健身中心、体能康复、训练辅导课、会议室、研讨间、击剑、轮滑、教工之家等 13 个 | ⚠️ 无 `.time`/`.siteList`，需用户手动操作 |

> 脚本的时段选择器已同时覆盖 `.siteList` 与 `.time`；遇到第三类场景会自动跳过并提示手动操作。

## 四、关键 API 速查

| 用途 | 方法 | 路径 |
|------|------|------|
| 检查登录用户 | GET | `/system/login/getLoginUser` |
| 获取 CAS 登录地址 | GET | `/cas/address` |
| CAS 回调换取 token | POST | `/cas/token` |
| 刷新 token | POST | `/system/login/refreshToken` |
| 运动项目（场景）列表 | GET | `/api/site/scene/list` |
| 场景详情 | GET | `/api/site/scene/detail` |
| 设备类型（取 devKindUuid） | GET | `/api/site/devKind/list`（参数 `uuid=<sceneUuid>`） |
| 场馆/楼栋（取 buildingUuid） | GET | `/api/site/chooseByType`（参数 `sceneUuid` + `siteType=BUILDING`） |
| 当日时段网格（真实查时段入口） | POST | `/api/reserve/current/page`（`resvKind=CURRENT_RESERVE`） |
| 跨天可约时段（部分场景） | POST | `/api/reserve/cross/detail`（参数 `reserveStartDate`/`reserveEndDate`/`siteUuid`/`siteType`） |
| 锁场 | POST | `/api/reserve/lockSite` |
| 提交预约 | POST | `/api/reserve/addReserve`（body 含 `captcha` 滑块 token，需先过 blockPuzzle 人机验证） |
| 我的预约记录 | POST | `/api/reserve/reserveRecord`（body `{pageNum,pageSize}`；记录含 `resvUuid`） |
| 取消预约 | POST | `/api/reserve/cancelReserve`（body `{resvUuid}`；需**提前 24 小时**，否则 `401000103`） |
| 运动推荐 | GET | `/api/reserve/recommend` |
| 场景是否支持抽签 | GET | `/api/lottery/scene`（`sceneUuid`） |
| 抽签场次列表 | POST | `/api/lottery/plans`（body `sceneUuid`+`siteIds`+分页） |
| 某场次报名详情（日期） | GET | `/api/lottery/plan/detail`（`lotteryUuid`） |
| 抽签报名提交 | POST | `/api/lottery/plan/instance`（body `{instanceUuids:[uuid]}`） |
| 我的抽签报名 | GET | `/api/lottery/instance/list`（`pageNum`+`pageSize`） |

> 注意：`/api/site/choose`（返回 500）与 `/api/reserve/current/period`（要求 `siteType` 却总报 `RESERVE_SITE_TYPE_NOT_EMPTY`）在本场馆**不可用**，已弃用；脚本改走 `chooseByType` + `devKind/list` + `current/page`。

完整参数与响应结构见 [`references/venue-api.md`](references/venue-api.md)。

## 五、环境准备（首次使用前执行一次）

1. 运行 `node scripts/venue-helper.js setup`，脚本会自动完成：
   - 在 skill 根目录生成 `package.json`，其中 `allowScripts` 为**对象格式** `{"playwright": true}`
     （不要写成数组 `["playwright"]`——npm 会解析成 `{"0":"playwright"}` 这类错键，不生效）
   - 安装前移除环境变量 `npm_config_allow_scripts`（DSH 注入的 allow-scripts 白名单会让 npm 11 抛 `EALLOWSCRIPTS`）
   - `npm install`（按 package.json 里固定 `1.62.1` 安装 playwright，**不会拉到最新**）
   - 浏览器**默认只用系统已装的 Chrome/Edge、不下载 Chromium**；系统都没有、或显式 `--with-chromium` 强制时，才 `npx playwright install chromium`
   - Chromium 下载默认走镜像加速，可用 `--mirror` 覆盖：`node scripts/venue-helper.js setup --with-chromium --mirror https://npmmirror.com/mirrors/playwright/`
2. 完成后运行 `node scripts/venue-helper.js login` 完成首次登录。

## 六、脚本约定（供 agent 稳定解析）

- 数据命令（`status`/`sports`/`sites`/`slots`/`recommend`/`orders`/`lottery` 的 plans/dates/signup/mine）都支持 `--json`，输出**单行机器可读 JSON**；不加 `--json` 时为人类可读文本。
- **数据查询命令不启动浏览器、零 playwright 依赖**：用 `login` 时保存的 token + storageState(Cookie) 直接发签名请求（Node 内置 `fetch`），**不会触发沙箱 EPERM，也不需要安装 playwright**；只有 `login`/`reserve`/`pay`/`open`/`lottery-open` 会启动浏览器并需要 playwright。
- `lottery signup` **默认只预演不提交**，必须显式加 `--yes` 才真正报名；`lottery` 子命令（plans/dates/signup/mine）都是无浏览器的纯 HTTP 操作。
- `cancel --uuid <resvUuid> [--yes]` 取消预约（封装 `/api/reserve/cancelReserve`，body `{resvUuid}`）：默认预演，`--yes` 才真正提交；**已实测成功取消**（取消后订单状态变为 `RESV_END`）。改约无独立简单接口（`/api/reserve/details/update` 依赖动态表单），当前走「取消旧预约 + 重新预约」或页面手动。
- `reserve-api --scene <...> --date <...> --time <...> [--yes --captcha <token>]` 纯 API 预约（body 已按 F12 实测报文校准）：默认预演；真正提交需 `--captcha`（前端有 blockPuzzle 滑块验证码，无法纯 API 绕过；且滑块 token 会被前端校验后立即消耗，难以单独取出）。**日常预约默认走 `reserve`（浏览器模拟，用户只需拖一次滑块）；`reserve-api` 仅作参考/验证码下线后的全自动方案，当前不可用。**
- 退出码：`0`=成功；`1`=失败；`2`=未登录/登录过期。判断登录态优先看退出码 `2` 或返回里的 `errorCode`。
- `slots` 直接按 `--scene` 查询，无需 `--site`（场地是动态的）；默认**从明天起查未来 3 天**（跳过慢的「当天」），`--date` 只查 1 天；多场景（如「西体羽毛球」→前馆/后馆）会自动合并，输出按「日期+时段」聚合为「N 片/场次」而非逐片罗列；**同时自动附带抽签报名场次**（若场景支持，`data.lottery` 字段）。场景名单（10 分钟）与场景上下文（devKind/building）都有本地缓存。
- `recommend` 支持 `--need "<需求描述>"` 按需求打分推荐（运动属性库：强度/关节冲击/目标/室内外/人数/价格），带匹配理由与 `score`；不带 `--need` 时走热度加权随机；`--scene 名称1,名称2` 可限定候选范围。推荐已按「运动」去重（如羽毛球多个场馆只出一次），并自动排除深水证/荣誉室/操房/会议室/研讨间/教工之家/训练辅导等非运动类场景。
- 脚本内置限流保护：遍历查询请求间隔 150ms；遇 `1610001 over limit` 自动指数退避重试（5s→10s→20s，最多 3 次）。
- 浏览器 profile 有互斥锁，并行运行多个命令会自动排队等待，而不是第二个实例启动失败。
- 场景上下文（`devKindUuid`/`buildingUuid`/`sceneUseType`）会缓存到 `~/.thu-sports-venue/scene-context.json`，命中缓存可省 3 次接口调用、缩短查询时间；若场馆配置变了，删除该文件即可强制刷新。

## 七、重要注意事项

- **必须先登录再查询**：几乎所有 `/api/*` 接口都需要 token，未登录返回 `errorCode 1130002`（登录过期）
- **登录走统一身份认证（清华 SSO）**：脚本直接打开 `https://id.tsinghua.edu.cn/f/login`，不要尝试账号密码直登或从场馆首页点「校内登录」
- **支付必须由用户本人在可见浏览器完成**（扫码），不要尝试代付或绕过
- **查询命令串行执行**：不要并行跑多个查询命令；接口限流有 ~90s 冷却，超限后等待冷却再继续（脚本已自动指数退避，但仍建议串行 + 间隔 ≥60s）
- **`sceneUseType` 按场景自动映射**：普通场馆（羽毛球等）为 `SPORT_GROUP`（包场），游泳等为 `SPORT_PERSON`（散客）；脚本按场景详情的位掩码（1=普通/2=包场/4=散客）自动取值，无需手动指定。若某场景查不到可约时段，可能是未到放票时间或已约满，向用户说明即可
- **只有交互命令才启动浏览器**：`login`/`reserve`/`pay`/`open` 会启动系统浏览器（登录/预约页/支付），在受限沙箱下可能触发权限审批；数据查询命令已改为无浏览器，不受影响。**升级后请先重新 `login` 一次**，以把 Cookie 保存到 storageState（否则查询仅靠 token，若服务端同时校验 Cookie 可能仍报未登录）
- **时段状态会实时变化**，预约前最好重新查询一次可约时段；如目标时段已满，向用户说明并给出替代时段
- **`recommend` 有两条路径**：带 `--need "<需求>"` 时按运动属性库（强度/关节冲击/目标/室内外/人数/价格）打分排序、附匹配理由与 `score`；不带 `--need` 时按热度加权随机抽取。两条路径都**不逐个查询实时可约量**、只附场馆信息，预约前必须用 `slots` 确认具体时段
- **非运动类场景**（深水证/荣誉室/操房/会议室/研讨间/教工之家/训练辅导课/体能康复中心等）没有 `devKind`/`building`，`sites`/`slots` 查不了；`recommend` 已**自动排除**这些场景，属正常现象
- **抽签报名与普通预约不同**：无滑块人机验证，纯 API 即可报名/查询，但**报名是真实动作**——脚本默认预演，须显式 `--yes` 才提交；抽签报名窗口外、已报名、或未开放报名的日期都会被拒绝
- **抽签报名查询也用接口限流**（`1610001`），`lottery` 数据命令与其它查询一样要串行、勿高频并行
- 站点响应可能被 AES 加密（字符串体）；脚本已自动解密，手工调用时注意先解密再解析
- 若脚本报 `MODULE_NOT_FOUND: playwright`，说明在跑**浏览器命令**（`login`/`reserve`/`pay`/`open`），执行 `node scripts/venue-helper.js setup` 安装一次即可；数据查询命令不需要 playwright

## 错误码速查

| errorCode | 含义 | 处理 |
|-----------|------|------|
| `0`（`code==0`） | 成功 | — |
| `1130002` | 未登录 / 登录过期 | 运行 `login` 重新登录 |
| `1610001` | `over limit` 请求过于频繁/超限 | 降低查询频率、等待冷却（脚本已内置 150ms 间隔 + 指数退避重试） |
| `2210007` | 参数缺失（如 `siteType` 为空） | 说明走了错误接口，改用 `current/page` + `CURRENT_RESERVE` |
| 其它非 0 | 业务 / 参数错误 | 读 `message` 字段排查 |

---

## 八、学生清华小程序 · 活动室申请（wechat-room）

帮用户查询/申请**学生活动室**（校团委管理，C 楼 / 南区 / 胜因院22号；开放时段：C楼/南区 7:00-23:00，胜因院 9:00-21:00）。已在 PC 微信（Weixin 4.x）的「学生清华」小程序里**逆向出后端 API**，用**接口直连**批量查询（一次拿到全部活动室），`Authorization: Bearer <jwt>` 鉴权。

> 适用情形：用户想「一次看全部活动室 / 查某活动室可用情况(占用) / 申请活动室 / 查我的活动室申请」时使用。
>
> 若用户需求未指明系统（只说「研讨间/讨论/借房间」），先走「零·甲 研讨间/房间 · 通用流程」两边对比 + 推荐，等用户确定选**活动室**再进入本节。

### 环境要求

- 需要 **PC 微信（Weixin 4.x）** 至少登录过一次「学生清华」小程序（用于产生/刷新 jwt）。
- 需要 **Python 3**（核心查询 `wechat-room-api.py` 仅用 stdlib、无需 Pillow；会话加密额外需要 `cryptography`，缺失时自动安装；仅 UI 兜底脚本 `wechat-room.py` 才需要 Pillow）。脚本读取运行中 `WeChatAppEx.exe` 进程内存提取 jwt。
- **`cryptography` 必装**（建议首次使用先 `pip install -r requirements.txt` 预装；缺失时首次运行会**自动 `pip install`**，装不上则报错并**拒绝明文保存** JWT，本次仅在内存使用、不影响查询/申请）：保存到本地的 JWT 会 **AES-256-GCM 加密**（密钥按本机宿主+用户名派生）。体育/图书馆/座位的 token/会话已用 Node `crypto` 加密存储。
- **无需常开小程序窗口**：查询/推荐/占用/可约/申请/撤销都是 API 直连，只需一个有效 jwt（在微信进程内存里，微信开着即实时提取；或已保存的 jwt，有效约 7 天）。**仅当 jwt 过期（~7 天）时**，才需在微信里打开「学生清华」小程序刷新一次。

### 第 0 步：检查前置（必做）

**先运行 `check`，确认登录态(jwt) 是否可用，再决定能否操作。**

```bash
python scripts/wechat-room-api.py check
```

`check` 输出：`wechat_running`、`miniprogram_open`（学生清华窗口是否在）、`jwt_available`、`jwt_source`（live=实时提取 / saved=兜底复用）、`ready_for_query`、`ready_for_apply`。

- **`ready_for_query` / `ready_for_apply` = 是否有有效 jwt**（查询/申请/撤销都是 API，无需小程序窗口）。
- `wechat_running` / `miniprogram_open` 仅作「刷新」参考：微信/小程序没开也能用已保存 jwt；`jwt` 约 7 天过期，过期时需在微信里打开「学生清华」小程序刷新一次。
- 若 `jwt_available=false`（无有效登录态）：引导用户打开并登录微信 → 打开「学生清华」小程序（脚本自动抓取并保存 jwt）。

### 主脚本 `scripts/wechat-room-api.py`（API 直连，推荐）

```bash
cd <skill根目录>
python scripts/wechat-room-api.py jwt                     # 从微信进程内存提取当前 jwt（含学号/过期）
python scripts/wechat-room-api.py list                    # 一次列出全部活动室(共51个；个别位置可能临时下架，以实时为准)
python scripts/wechat-room-api.py list --json             # 机器可读 JSON
python scripts/wechat-room-api.py occupancy C200          # 查 C200 的占用记录(申请中/已通过时段)
python scripts/wechat-room-api.py occupancy <房间id> --json
python scripts/wechat-room-api.py recommend --capacity 10 --type 会议   # 按容量/用途/位置推荐
python scripts/wechat-room-api.py recommend --location 胜因院 --max 12 --json
python scripts/wechat-room-api.py recommend --purpose 会议 --capacity 10   # 按「活动用途」匹配房间用途（一般用途自动避免录音/导播/舞蹈/排练等专用功能房；专用用途如 排练/舞蹈/录音/拍摄 则匹配专门房）
python scripts/wechat-room-api.py recommend --purpose 排练
python scripts/wechat-room-api.py avail --capacity 10 --type 会议 --from 2026-08-31 --to 2026-09-06   # 高自动化：按需求+日期区间直接给可约时段
python scripts/wechat-room-api.py avail --capacity 15 --location 胜因院 --from 2026-08-31 --to 2026-09-06 --json
> ⚠️ **`recommend` / `avail` 的 `--type` 与 `--purpose` 都是有效参数**（脚本两者都读）：`--type`=按类型关键词匹配、`--purpose`=按用途/功能特征匹配；若同时给，`purpose` 优先。**别因其中一个查 0 条就误判「参数用错了」**——0 条通常是容量/类型/日期过滤太严，放宽或去掉过滤再试。
python scripts/wechat-room-api.py apply --room C200 --date 2026-09-08 --start 10:00 --end 12:00 \
    --organizer "主办方（单位+姓名）" --participants 3 --subject 主题 --usage 内容 --type 文化活动   # 默认 dry-run；--yes 才两步提交
python scripts/wechat-room-api.py cancel --ticket <ticketId>   # 撤销申请（ticketId 取 apply 返回 / 我的申请）
python scripts/wechat-room-api.py mine        # 列出我的申请（申请中/被拒，不含草稿；草稿看 drafts）
```

- **接口基线**：`https://student.tsinghua.edu.cn/v2/api`
- **鉴权**：`Authorization: Bearer <jwt>`（jwt 由读微信进程内存自动提取，无需手动）
- **数据接口**：`GET /activity/rooms`（全部活动室，实测 count=51）；`GET /activity/rooms/:id/records`（单室占用）；`/activity/rooms/filters`、`/account/departments`、`/account/semesters` 等。
- 完整接口/字段/鉴权规范见 `references/wechat-room-api.md`。

### 鉴权说明（重要）

- jwt 来自 `POST /activity/wechat/login`（body `{code}`=wx.login 的 code），存于 `getApp().globalData.jwt`，也出现在微信**主进程(WeChatAppEx main) / 网络进程(network)** 内存里（**ASCII 单字节**存储，搜 `eyJ...` 即可）。
- jwt payload 含 `card`(学号)、`openId`(微信 openid)、`iat`/`exp`；**有效期约 7 天**，与当前微信/学号登录会话绑定。
- 脚本每次调用前自动从微信进程内存提取 jwt；若提取失败（退出码 2），请用户先打开「学生清华」小程序登录。

### 自动申请（apply = API 两步提交，已抓包验证）

查询用 API；**申请也用 API**（`POST /activity/room-tickets` 创建 ticket → `POST /activity/room-tickets/<id>/apply` 提交），已通过抓包确认真实 schema。

```bash
python scripts/wechat-room-api.py apply --room C200 --date 2026-09-08 --start 10:00 --end 12:00 \
    --organizer "主办方（单位+姓名）" --participants 3 --subject 主题 --usage 内容 --type 文化活动 \
    [--need-meal --need-film --is-big-event]   # 涉及用餐/观影/重要会议，默认都不勾
# 默认 --dry-run 只构建请求；--yes 才真正提交（提交前自动校验该时段是否可约，冲突则拒绝，--force 绕过）
```

- **提交前校验**：先查 `/activity/rooms/:id/records`，若目标时段已被占用/申请中则拒绝，避免创建冲突 ticket。
- **body 关键字段**（抓包确认）：`records=[{type:0, roomId:<房间UUID>, semesterId:<学期UUID>, start:毫秒, end:毫秒}]`、`participantCount` 为**字符串**、`activityTarget`（全校通用 `school`）、`activityType`（合法值映射：党团活动=party、社团活动=organization、体育赛事=sports、学术报告=academy、文化活动=culture、文艺活动=art、其它活动=others）。
- **ticketId** 在第一步响应的 `Location` 头（不是 body）；`/apply` 步骤返回 204 即成功。
> ⚠️ **对用户绝不暴露命令/参数/内部排查**：即使内部在跑 `check`/`apply`/查参数，也不要对用户说「我跑一下 check」「确认 apply 参数」「参数都清楚了」——用户只需听到自然语言结果（如「登录态正常」「可以提交，你确认一下」）（详见「沟通原则」）。
- **活动室申请 · 标准流程（每次固定按这几步执行，不要随现场变）**：
  1. **确认需求**：多少人？哪天？**上午/下午/晚上（笼统时间即可）**？**大概活动多久（单次≤2h，如 1h/2h）**？办什么（用途）？有无位置偏好？
  2. **查房间**：`list` 或 `recommend --purpose 用途 --capacity N`（按用途匹配，自动避开录音/导播/舞蹈/排练等专属功能房；`--location 胜因院` 等可按位置）。**「小组讨论/研讨/开会」这类要能匹配「小型研讨室/多功能研讨室/会议室」**——用 `--purpose 讨论`（或 `--type 会议/研讨/讨论`），别用只命中「会议」的窄关键词，否则会漏掉 2 人「小型研讨室」。
  3. **查可约**：`avail --capacity N --purpose 用途 --from D --to D`（**已按各位置规则自动过滤**：胜因院22号 需**提前≥3天/9:00-21:00**；C楼/南区地下 **≥4h/7:00-23:00**；均 ≤14天；日期不满足则不会出现该位置），或 `occupancy <房间名>`（看单室占用）。**同时查「该日期是否已有申请」**（`mine` 看申请中/被拒）——若该日期已有一笔「申请中」，**主动提示用户「你该日期已有一笔申请，每天最多 1 次、不能再约」**，别让他白走一遍选房/收集。
  4. **选房**：在该**笼统时间**范围内用 `avail` 看可约时段，挑出 **人数/日期/精确起止时间** 都合适的房间+时段（可给 1~2 个精确方案供选），报给用户确认。**同时给用户「让我随便选/你帮我定」的免操作选项**——若用户说「随便/都行」，就按最合适（容量恰好、用途匹配）挑一个；**人数少时优先挑容量刚好、更匹配的小房间**（大房间人少更可能不通过）。**若选定房间容量明显大于实际人数（如 4 人→9 人房间），优先换更匹配的小房间，如实申报人数、不得虚报填高**（见注意事项）。
  5. **收集字段**（逐项问答，见下）。
  6. **汇总确认**：把 房间/日期/时段/主办方/主题/人数/类型/内容 列给用户确认。
  7. **提交**（真实动作）。**提交成功后，主动告知用户后续可操作（自然语言，不抛命令名）**：
     - **查看**：可以帮你查我的申请（申请中/被拒；草稿箱单独看）。
     - **撤销**：可以帮你撤销这笔（会自动清掉草稿，无需你操心）。
     - **修改**：系统没有「直接修改」，要改的话撤销旧的后重新提交一份即可。
     - **等审核**：提交后审核，通过后微信通知、可看密码锁；审批结果最晚「使用日期前 3 天」（胜因院）/ 按通知告知。
     - 若用户不清楚，可用一句话概述：「已提交【房间 + 日期时间】的申请；我可以帮你查、撤销（会自动清草稿）、修改（撤销后重新提交），草稿箱也能看。」
- **每步结束都主动告知「下一步可做什么」**（别让用户猜；**用自然语言，不抛命令名**）：确认需求后→「我可以帮你推荐房间、查可约」；查完可约→「可以选某个时段、换日期、或换房间」；选好房间→「可以帮你提交，提交前我会汇总确认」；提交后→「可以查看、撤销（自动清草稿）、修改（撤销后重新提交）」；改/撤之后→「可以重新申请或继续查别的」。
- **表单字段（含灰字提示 = 每项该填什么的格式提示）**：
  - 【主办方】*——灰字「请输入主办方（单位+姓名）」→ 填「单位+姓名」，如「主办方（单位+姓名）」。
  - 【活动主题】*——灰字「请输入活动主题」→ 给个具体题目（留空会报「该项不能留空」）。
  - 【参与人数】*——灰字「请输入活动人数」→ 数字，≤ 该活动室容量。
  - 【活动类型】*——灰字「选择活动类型」→ 下拉（**可往下滚，共 7 类**：党团/社团/体育赛事/学术报告/文化活动/文艺活动/其它活动），按活动性质推荐。
  - 【涉及用餐】【涉及观影】【重要会议】——勾选，默认否。
  - 【活动内容】*——灰字「请描述活动具体内容，否则将无法通过申请」→ agent 起草**初稿** → **用户给意见 → 你润色 → 再请用户确认；不满意继续润色确认，直到用户满意**；**内容不重复表单已填**（主题/主办方/人数/日期/类型），只写具体活动安排。
- **向用户提问（用 `question` 提问框一次列出几个问题字段、用户依次逐个回答；问题顺序固定如下，但不要把「固定顺序/最高效」这类内部说明写进给用户的回复；**每问一项都给出比表格更丰富的信息**——列出全部选项、给格式/示例，不漏表格已有的内容）**：
  1. **日期 + 笼统时间 + 大概时长 + 参与人数**——**这些在「确认需求」时已问过，若已收集则不要重复问**；未收集才问。先问清几件短事实；**起止时间先笼统**（上午/下午/晚上/大致范围），**同时确认大概活动时长**（**单次≤2h**，如 1h/2h），如「想申请哪天？上午/下午/晚上？大概活动多久（2 小时内）？多少人？」；**精确起止时间按「时间+时长」去 `avail` 里找满足的可约时段**确定，选房时报给用户确认。注意：若用户要**超过 2h**，提示**超限**（单次≤2h、**每日 1 次**——同一天不能再约第 2 次，要更久只能**跨天各约**）。人数按实际填写（见注意事项）。
  2. **主办方**——`config org` 已存则只确认一次（「主办方还是【…】，对吗？」）；未存则问「主办方单位+姓名？」。
  3. **活动主题（必先问清，它是后面类型推荐和内容初稿的依据）**——**在问类型、起草内容之前，必须先确定主题**；用户没想好，agent 给个具体题目，让用户确认/改。
  4. **活动类型**——**必须在已知道主题之后**，再按活动性质**推荐**并列出全部 7 项（①党团活动 ②社团活动 ③体育赛事 ④学术报告 ⑤文化活动 ⑥文艺活动 ⑦其它活动），同时 agent 按活动性质**推荐一项**（如「我推荐选【学术报告】」），**必须向用户确认**（「还是你想选别的？」）。**推荐只是建议，最终以用户选择为准**，不能直接代选（推荐映射见下）。
  5. **活动内容**——**必须在已知道主题之后**，agent 起草**初稿** → 用户给意见 → **润色→再确认**（不满意继续润色确认）→ 以用户为准；**不重复表单已填内容**，只写具体活动安排。
  6. **涉及用餐 / 涉及观影 / 重要会议**——一句话问「三个都默认「否」，涉及吗？」（通常否）。
  7. **汇总确认**——把 房间/日期/时段/主办方/主题/人数/类型/内容 列给用户确认，再 `apply --yes`。
  - **活动类型 · 按性质推荐**：学术讲座/报告/答辩/科研交流→**学术报告**；文艺演出/晚会/才艺/艺术→**文艺活动**；文化展览/读书/文化交流→**文化活动**；社团例会/兴趣分享→**社团活动**；体育比赛/运动→**体育赛事**；党团学习/主题团日→**党团活动**；无法归类→**其它活动**。**推荐后必须向用户确认（以用户选择为准），不能直接代选**。
  - **活动内容 · 写法（申请表的正式口吻，但别写成 AI 腔）**：**不重复**表单已填的 主题/主办方/人数/日期/类型/**时长**；**完整陈述句、正式动词**（计划/讨论/使用/展示/交流），但**不要用 ①②③ 分点、不要用「议程/环节/产出/技术路线/实现难点」等空词，也不要太口语（「弄下/聊聊」这类）**。示例：「我们小组计划讨论 AI 创新大赛参赛项目的选题和分工安排，需要使用房间的电视投影展示相关资料。」（避免「课程讨论/读书交流/例会」等笼统词被拒）。——**润色-确认迭代**：用户给意见后你润色，再请用户确认；不满意继续润色、再确认，直到用户满意。
- **`--yes` 是真实动作，提交前先向用户确认明细**（房间/日期/时段/主办方/主题/人数/类型/内容）。
- 完整 schema 见 `references/wechat-room-api.md`（第七节）。

> 备用：若某动作不走 API（如特殊流程），用 `wechat-room.py`（`click/scroll/capture`）驱动「导航到表单 -> 用户填」。`activityTarget=school` 通用（C楼/南区/胜因院），`activityType` 已全部映射（党团=party/社团=organization/体育赛事=sports/学术报告=academy/文化活动=culture/文艺活动=art/其它活动=others）。

### 修改已提交的申请（用户要求改时，先复核再执行）

**最优先原则：修改时默认其他条件不变**——时间/日期/房间/人数/类型/内容/主办方等，**只改用户明确要改的那一项**，其余一律保持原申请的值；不要擅自「顺手优化」其他条件。先确认「要改哪一项」，再只针对该项复核。**但有个例外**：若改的那一项**导致原房间/时间不再匹配**（人数大减→原房间过大、改时间→原房间该时段已被占用、改用途→原房间是专用功能房不匹配、改位置→换地点），则**必须主动查可约（`avail`/`recommend`）并询问用户是否换房/换到更匹配的**，不能因为「默认不变」就继续用不匹配的原房间。

开发**没有「修改」接口**——改一笔申请 = **`cancel` 掉原申请 + 重新 `apply`**（两步：撤销旧、提新）。用户说"要改申请"时，先**复核以下**再执行（撤销是真实动作，先向用户确认）：

1. **先定位是哪笔**：`mine` 列出「申请中/被拒」（**不含草稿箱**；草稿箱用 `drafts`），确认用户要改的那笔的 ticketId。
2. **复核房间要不要换**（尤其 人数/用途 变了时）：
   - **人数变了（增多或减少）** → 重查容量：`recommend --capacity N`，原房间容量还够吗？不够就换；**人数变少了，必须主动查更小、更匹配的房间并列出候选、询问是否换房**（大房间人少更可能不通过）——**如实申报人数，优先换更匹配的小房间**。
   - **用途变了** → 重查用途↔房间匹配：`recommend --purpose 新用途`，原房间（或录音/导播/舞蹈/排练等专用房）还匹配吗？改用途可能要避开专用功能房。
   - **位置偏好变了** → 加 `--location` 按位置重查。
   - **换房间时，列出候选房间的完整信息**（**名称 / 位置 / 容量 / 用途标签 / 部门 / 可约时段**），让用户对比后选，**不要只说「换一个更匹配的」而不给具体房间信息**。**同时逐项核对新房间是否满足原有条件，不满足的要明确说明**（例如：原定时段在新房间已被占用→「需同时改时间」；新房间容量小于原人数→「容量不够」；新房间是录音/导播/舞蹈/排练等专用功能房→「用途不匹配」；新房间位置规则不同→「如胜因院需提前≥3天/9-21」）。
3. **复核时间/日期**（时间/时长变了时）：按**新时长**用 `avail`（房间+区间）或 `occupancy` 挑满足时长且可约的时段；确认在开放时段（C楼 7:00-23:00 / 胜因院 9:00-21:00）、满足 提前≥4h、距开始≤14天、**单次≤2h**（要更长需提醒超限）、**每日 1 次**。
4. **复核其它字段**（主题/类型/内容/主办方变了时）：同新建——类型按性质推荐须用户确认、内容起草初稿→润色→确认、不重复已填；主办方复用 `config org`。
5. **执行 = 撤销旧 + 提新**：确认改动无误后，`cancel --ticket <原ticketId>`（**已自动撤销并删除草稿**）→ **用「原申请的值 + 只改的那一项」直接 `apply` 提交新的，不必重问所有字段**（其余沿用原申请）；**提醒用户旧的那笔会失效、新的是真实动作**。
6. **结果确认**：改完后 `mine` 复核新申请在「申请中/被拒」（草稿箱看 `drafts`）。

> 同理，用户说"取消申请"→ 走 `cancel`（可先 `mine` 定位）；"看看我申请了什么"→ `mine`。

### UI 自动化（降级/备用脚本 `scripts/wechat-room.py`）

若某动作无法走 API（如实名/验证/特殊流程），用 `scripts/wechat-room.py` 做**截图 + DPI-aware 坐标点击**驱动窗口：

- `locate` / `capture <png>` / `click <x> <y>`（图像坐标，已过 150% 缩放 DPI 校正）/ `scroll <dir> <times>` / `type <text>` / `config org <v|get>`。
- 需 `SetProcessDPIAware()`（150% 缩放 1.5x），截图/点击均物理像素，图像坐标与窗口 1:1；`SetWindowPos(TOPMOST)` 防遮挡。
- 此路径为「逐房间导航」，效率低，**仅作兜底**；批量查询优先用 API 脚本。

### 注意事项 / 限制

- **活动室**：仅限学生/教职工/校友；预约须提前 ≥4h、距开始 ≤14 天；**每人每日 1 次、单次 ≤2h**；开放 7:00-23:00（C楼/南区，不含寒暑假；**胜因院 9:00-21:00**）；提交后审核，通过后微信通知，可看密码锁（`/admin/lock/keyboards/*`）。
- **人数「虚报」已移除（如实申报为原则）**：参与人数**按实际填写**（≤ 房间容量）。若选定房间容量明显大于实际人数（如 4 人→9 人房），**优先换更匹配的小房间**，而不是填高人数。推荐/修改时始终**以最符合实际人数的房间为准**。
- **「单次≤2h / 每人每日 1 次」是使用/审核规范，不是接口硬限制**：接口 `records[]` 是数组、`/records/check` 对「单条>2h」「同一天多条连续」都返回 available、提交也能 200——但**审批大概率不通过**。所以：**同一天连续预约两个时间段（2h+2h）违规（每日1次）**，不建议；**单场>2h 审不过**；**跨天**（各每日1次）才可以。胜因院为审批制（9-21、≥3天、禁娱乐、登记），多时段也可能受限、按审批。
- **`cancel` 会「撤销 + 自动删除草稿」**（固定步骤，无需再手动 remove）：先 `revoke`（已提交→204，草稿→412），再 `remove`（204 删除草稿）；两者其一成功即视为已处理。**`mine` 只列「申请中/被拒」（不含草稿箱）；看草稿箱用 `drafts`，删草稿用 `remove --ticket <id>`**。
- **jwt 会过期**：约 7 天，且绑定当前微信会话；过期时重新打开小程序登录即可，脚本会自动提取新 jwt。
- **注意**：`https://open.fdep.cn` 只是埋点/监控域名，**不是**业务后端；业务后端是 `student.tsinghua.edu.cn/v2/api`。

---

## 九、图书馆 IC 空间（`cab.lib.tsinghua.edu.cn`）

> 【来源分流】本节省自合作伙伴 skill `thu-lib-space-reserve`（thu-auto-reserve-1）。脚本 `scripts/space-helper.js` + `scripts/fetch-notices.js`；参考 `references/lib-space-api.md` / `references/lib-space-spaces.md` / `references/lib-space-notices.md`。
>
> 若用户需求未指明系统（只说「研讨间/讨论/借房间」），先走「零·甲 研讨间/房间 · 通用流程」两边对比 + 推荐，等用户确定选**图书馆研讨间**再进入本节。

### 关键事实
- 站点：`https://cab.lib.tsinghua.edu.cn/#/ic/home`（Vue SPA）；API 基线 `https://cab.lib.tsinghua.edu.cn/ic-web`
- 覆盖：**研讨间 / 座位 / 考研座位 / 活动 / 外借设备 / 电子阅览室**
- 登录：清华统一身份认证（SSO）；token 在 `sessionStorage.userInfo.token`（JSON 字符串需 `JSON.parse`）
- 请求：**无签名、无 AES 加密**，明文 JSON `{code,message,data}`；`code==0` 成功、`code==300` 未登录
- 登录态持久化在 `~/.thu-lib-space/`

### 标准工作流
0. **确保登录**：`node scripts/space-helper.js status`（未登录则 `login`；换号 `login --force`；绑邮箱 `bind-email --email <邮箱>`）
1. **判断意图**：不知道约什么→第2步；知道类型不知房间→查 `references/lib-space-spaces.md` 推荐；知道房间不知时间→第3步；知道房间+时间→第4步
2. **推荐空间 + 收集信息**：`menu --json` + `references/lib-space-spaces.md`（小组讨论→研讨间、自习→座位、备考→考研座位、需电脑→电子阅览室/外借设备）。**约「研讨间」时用 question 提问框一次列出字段收集**：人数、哪天/几点/用多久（30 分–4h）、用途（小组讨论/面试/答辩/自习/备考/音乐练习）、位置偏好（北馆/文科/西馆音乐/法律馆，或随便）、成员学号（预约时需填）；**填成员时告知用户「系统自动把当前登录账号算作一位成员，成员学号只需填其它同学」**（若用户误填了本人学号，脚本会自动去重）；其它类型（座位/考研座位/设备）自然问答。
3. **列可约时段**：`slots --room <类型> --date <日期> --json`（已含各房间编号/人数/空闲时段），用 **Markdown 表格**呈现。**用户提出时间范围后，先对照「预约规则要点」校验时长上限（双人舱 ≤2.5h / 其它 ≤4h）、开放时间、提前天数是否可行，再收集成员学号/提交**——不要等到最后提交预演才发现超时而返工。
4. **预约（纯 API）**：先对照 `references/lib-space-spaces.md` 逐项校验规则（违反→拒绝并指出哪条；脚本会自动校验提前天数/开放时间/时长/人数），再 `reserve --room <类型> --dev <房间> --date <日期> --time HH:mm --end HH:mm --members 学号1,学号2`；默认**预演**、确认后 `--yes` 才真正提交（脚本用 `account/getMembers` 把成员学号转成证号、组装 body 提交，**无需用户开浏览器**）。若某场景必须走页面，加 `--browser` 回退浏览器 UI（用户在弹窗核对成员+时间后手动提交）。
5. **查/详情/取消**：先 `orders` 列出我的预约（拿到**预约号** + 房间名），再 `detail --uuid <预约号|uuid>` 看详情、`cancel --uuid <预约号|uuid> [--yes]` 取消。**⚠️ `cancel`/`detail` 的 `--uuid` 只认「预约号」或「系统 uuid」，不认房间名（如「北馆3F-09」）；取消前必须先用 `orders` 定位到那笔的预约号，再用预约号取消——不要拿房间名当 uuid 提交**。

### 预约规则要点（第4步校验必查）
- **提前天数**：单人研读间 N=3（最晚今天+2天）；团体/音乐/研讨舱/双人舱 N=5（最晚今天+4天）
- **开放时间**：研读/研讨/双人舱 08:00–22:00；音乐研讨间 08:30–21:30
- **单次时长**（30分起）：单人/团体/研讨舱/音乐 30分–4h；双人舱 30分–2.5h
- **人数**：单人1；北馆团体3–10；文科团体3–8；高山音乐5–18；流水音乐3–9；研讨舱2–4；双人舱1–2
- **每日上限**：同一天最多 1 个有效预约（用 `orders` 核对）

### 注意事项
- 必须先登录再查询（未登录 `code 300`）；只走 CAS，不试账号密码直登
- **提交预约**：默认纯 API 提交（无需浏览器）；提交前须先向用户确认（房间/时段/成员），`--yes` 才实际提交，不代用户擅自提交；`--browser` 才回退浏览器 UI（用户手动操作）。
- 违约影响信用分，提醒按时到馆、无法到馆及时取消
- **取消提示要准确**：图书馆研讨间是「**每天最多保留 1 个有效预约**」，**取消后可再约**（只要当天无其它有效预约、且仍有可约时段）——**不要告知「取消后当天就不能再约别的」这种错误信息**；「取消每日限 1 次」是自习座位系统（第十节），不属于研讨间。
- 隐私边界：不透露接口路径/参数/DOM选择器/错误码/token；不存储回显手机号/邮箱/卡号/身份证
- **对用户绝不暴露命令/参数/内部排查**（如「我先跑 status」「查 reserve 参数」）——只讲自然语言结果（详见「沟通原则」）。
- 完整接口/错误码见 `references/lib-space-api.md`

## 十、图书馆自习座位（`seat.lib.tsinghua.edu.cn`）

> 【来源分流】本节省自合作伙伴 skill `thu-lib-seat-reserve`（thu-auto-reserve-2）。脚本 `scripts/seat-helper.js`；参考 `references/lib-seat-api.md` / `references/lib-seat-seats.md`。

### 关键事实
- 站点：`https://seat.lib.tsinghua.edu.cn/home/web/f_second`（ThinkPHP 服务端渲染 + jQuery，无 SPA）
- **只负责普通自习座位**（当日/次日预约）；研讨间/考研座位/活动/设备 在第九节（图书馆 IC 空间）
- 会话：Cookie（`PHPSESSID` + `access_token` + `userid` + `user_name` + `expire`）；无签名、无 AES、无 token 头
- 登录：CAS 单点登录（`/cas/index.php?callback=`）；登录态持久化在 `~/.thu-lib-seat/`
- 预约范围：当日/次日；系统开放 6:00–23:00；免费无支付
- 馆舍 id：北馆(李文正馆)=35、西馆(逸夫馆)=64、文科图书馆=89、法律图书馆=6、美术图书馆=19、金融图书馆=29

### 标准工作流（固定流程，**不要直接问「选哪个图书馆/楼层」**）
0. **确保登录**：`node scripts/seat-helper.js status`（未登录则 `login`）
1. **先确认借阅需求**：问「要不要顺便借书/查专业资料？」——有 → **按藏书定位图书馆**：理工科→西馆；中文社科→北馆；人文社科→文图；法学→法图；艺术→美图；经济金融→金融图书馆；建筑→建筑图书馆（**不在座位系统，先到先得**）。定位后直接进第 4 步。
2. **无借阅需求 → 列「自习条件」问要不要特别条件**（自然语言列可选项，不是让用户报馆名）：
   - 需要电脑/上机 → 文科馆一层「信息空间」（电脑座 50 余台）
   - 需要外接大屏/扩展屏 → 文科馆一层信息空间南侧、二层 A 区
   - 需要绝对安静/独立小间 → 法律馆「静音舱」或「单人研读间」（法图四层北区）
   - 想顺手翻专业书 → 西馆「书架间自习座」
   - 人少、很安静、座位好约 → 金融图书馆（需出示学生证、可能登记）
   - 离宿舍近 → 北馆（紫荆附近）
   - 不想预约、想直接去 → **老馆**（无需预约，约 476 席，7:30–22:30，考试周至 24:00）
   选中条件 → 直接确定图书馆+区域，进第 4 步。
3. **都没特别需求 → 给「随便选」+ 推荐最优**：主动给「随便选 / 你帮我定」选项；默认推荐**北馆二~五层**（座位多、环境舒适、离宿舍近，自习条件最好）或**西馆**（理工专业书+常用自习）；不想预约则推荐**老馆**。
4. **确定后列出所有可选区域 + 给「随便选」**：`tree --lib <馆舍id> --json` 列出该馆舍所有可在线预约的楼层/阅览区（含实时空闲）让用户选，同时给「随便选」；**用户选「随便选」时，优先挑空座位比例最多的阅览区**；确定后 `open --section <阅览区id> [--date]` 打开选座页，用户点选绿色空闲座位完成。
5. **查/取消**：`bookings --json`（纯 HTTP 列出我的预约，含取消用 id）或 `orders`（打开我的预约页）；`cancel --yes`（自动取消第一条）或 `cancel --id <取消id> --yes`（默认预演，每日限取消 1 次）

### 预约方式说明
- 预约一律「打开选座网站让用户手动点选」，脚本 `reserve` 仅调试保留、正常不调用
- 预约成功须 30 分钟内刷卡签到，否则释放座位并记违约

### 注意事项
- 违背规定「拒绝+等待反馈」：开放时间外(非6:00–23:00)、超出当日/次日、现场选位区、美术馆闭馆 → 拒绝并说明
- 取消每日限 1 次；违约满 5 次暂停 3 天（每学期初 3/1、9/1 清零）
- 本系统只约普通自习座位，研讨间/考研座位用第九节
- 浏览器受限时不装 Chromium、不走程序化预约，请用户放宽权限用系统 Edge/Chrome 手动选座
- **对用户绝不暴露命令/参数/内部排查**（如「我先跑 areas」「查 cancel 参数」）——只讲自然语言结果（详见「沟通原则」）。
- 完整接口/错误码见 `references/lib-seat-api.md`

---

## 九·十 通用沟通/UX 原则（同活动室分支，适用于图书馆 IC 空间/自习座位）

- 对用户只讲**自然语言**，不暴露内部命令/脚本/接口（全局「沟通原则」适用于所有四个分支）。
- **每步结束主动告知「下一步可做什么」**：查完空间/座位→「可选房间/时段、换馆舍/日期」；选好→「可帮你预约，预约前我会让你确认」（图书馆 IC 空间=纯 API 预演后确认；自习座位=打开选座页你点选）；预约后→「可查、可取消」。
- **提交/取消是真实动作**：默认预演，`--yes` 才执行；按实际情况告知（能否取消、违约规则），不盲目承诺。
- 图书馆**研讨间**用 question 提问框收集信息（同活动室，见第九节）；图书馆**自习座位**自然问答即可。

## 来源与归属（分流标注）

| 来源 | 文件 | 功能 |
|------|------|------|
| 本人（thu-auto-reserve） | `scripts/venue-helper.js` | 体育场馆 |
| 本人（thu-auto-reserve） | `scripts/wechat-room-api.py` / `scripts/wechat-room.py` | 学生活动室 |
| 合作伙伴（thu-auto-reserve-1 = thu-lib-space-reserve） | `scripts/space-helper.js` / `scripts/fetch-notices.js` | 图书馆 IC 空间 |
| 合作伙伴（thu-auto-reserve-2 = thu-lib-seat-reserve） | `scripts/seat-helper.js` | 图书馆自习座位 |
| 本人 | `references/venue-api.md` / `references/venues.md` / `references/mini-program.md` / `references/activity-rooms.md` / `references/wechat-room-api.md` / `references/lessons.md` / `references/room-compare.md` / `references/venue-resource-handbook.md` | 体育/活动室/经验/研讨间对比/场地资源手册 |
| 合作伙伴1 | `references/lib-space-api.md` / `references/lib-space-spaces.md` / `references/lib-space-notices.md` | 图书馆 IC 空间参考 |
| 合作伙伴2 | `references/lib-seat-api.md` / `references/lib-seat-seats.md` | 图书馆自习座位参考 |
